#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print all number digit
 *
 * Return: always 0 (success)
 */

int main(void)
{
int i;
for (i = 0; i <= 9; i++)
{
printf("%d", i);
}
return (0);
}
