#ifndef SUM_H
#define SUM_H

#define SUM(x, y) printf("%d + %d = %d", x, y, x + y)
#include <stdio.h>

/**
 * main - sum of function
 *
 * Return: Always 0 success
 */

int main(void)
{
	SUM(5, 6);

return (0);
}

#endif /*SUM_H*/
